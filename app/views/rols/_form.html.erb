<%= form_with(model: rol, local: true) do |form| %>
  <% if rol.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(rol.errors.count, "error") %> prohibited this rol from being saved:
      </h2>

      <ul>
        <% rol.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>
      <div class="container">
        <div class="row">
          <div class="col s3"></div>
          <div class="col s6">
            <div class="field">
              <%= form.label :descripcion %>
                <%= form.text_field :descripcion %>
            </div>

            <div class="btn waves-effect waves-light">
              <%= form.submit "Guardar" %>
            </div>
            <% end %>
          </div>
          <div class="col s3"></div>
        </div>
      </div>